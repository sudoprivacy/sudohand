//! `praxis-browser` — browser actuator (desktop's sibling).
//!
//! Drives Chrome over the DevTools Protocol: navigate, click/type,
//! read the page, capture screenshots, manage cookies/dialogs.
//! Cross-platform.
//!
//! **To be ported in from `ai-dev-browser` (adb):** the CDP client,
//! `browser`/`chrome`/`connection`/`actions` modules, cookies, etc.
//! Until then this is a placeholder so the workspace builds.

/// Placeholder until adb is ported in.
pub fn placeholder() -> &'static str {
    "praxis-browser: port adb here"
}
